language: python

python:
  - "3.5"
  - "3.6"
  - "3.7-dev"

install:
  - pip install -e .
  - pip install pytest-cov
  - pip install codecov

script:
  - python setup.py test
  - py.test --cov=./

after_success:
  - codecov

